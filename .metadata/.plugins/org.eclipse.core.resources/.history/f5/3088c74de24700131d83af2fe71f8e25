package hearsay.listener;

//import server.Message;

import server.SocketProcessor;

/**
 * Represents a SocketProcessorListener in the hearsay system
 * @author Subhasis Das
 *
 */

public interface SocketProcessorListener {

	void onDisconnect(SocketProcessor sp);
	void onReceive(SocketProcessor sp, String message);
}
